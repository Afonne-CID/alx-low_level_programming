#include "holberton.h"

/**
 * print_line - Entry point
 * 
 * @n: paramater to be passed to function
 * Return: Always 0
 */
void print_line(int n)
{

  if (n <= 0)
    {
      _putchar('\n');
    }
  else
    {
      _putchar((95 * n) + '0');
    }
  
}

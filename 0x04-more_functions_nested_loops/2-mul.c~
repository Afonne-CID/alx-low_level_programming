#include "holberton.h"

/**
 * mul - Entry point
 *
 * @a: First paramater to pass to function
 * @b: Second paramater to pass to function
 * 
 * Return: Always 0
 */
int mul(int a, int b)
{
	if ((a == int)&&(b == int))
	{
		result = a * b;
		return (result);
	}
	else
	{
		return (0);
	}
}
